# data

## CSV Files
Use the CSV files and the `neo4j-admin` tool to start a local Neo4j instance of the CHCD.

##Dumpfile
Use the CSV files and the `neo4j-admin` tool to start a local Neo4j instance of the CHCD.
